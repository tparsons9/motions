name: Deploy docs

# Manual-only on this personal fork: not auto-publishing a docs site on every doc change.
on:
    workflow_dispatch:

permissions:
    contents: read
    pages: write
    id-token: write

concurrency:
    group: pages
    cancel-in-progress: false

jobs:
    build:
        runs-on: ubuntu-22.04
        steps:
            - name: Checkout this repo
              uses: actions/checkout@v4
              with:
                  fetch-depth: 0

            - name: Checkout Quartz v5
              uses: actions/checkout@v4
              with:
                  repository: jackyzha0/quartz
                  ref: v5
                  path: quartz

            - name: Generate changelog docs page
              run: |
                  mkdir -p docs/reference
                  {
                      echo '---'
                      echo 'title: Changelog'
                      echo 'description: All notable changes to Vim Motions, following Keep a Changelog format.'
                      echo 'tags:'
                      echo '  - reference'
                      echo '---'
                      echo ''
                      echo '> See individual releases on [GitHub](https://github.com/saberzero1/motions/releases) for per-version discussion and asset downloads.'
                      echo ''
                      cat CHANGELOG.md
                  } > docs/reference/changelog.md

            - name: Generate known limitations docs page
              run: |
                  {
                      echo '---'
                      echo 'title: Known limitations'
                      echo 'description: Known limitations, architectural constraints, and intentional design decisions in Vim Motions.'
                      echo 'tags:'
                      echo '  - reference'
                      echo '  - limitations'
                      echo '  - troubleshooting'
                      echo '---'
                      echo ''
                      cat KNOWN_LIMITATIONS.md
                  } > docs/reference/known-limitations.md

            - name: Inject content and config
              run: |
                  rm -rf quartz/content
                  cp -r docs quartz/content
                  cp quartz.config.yaml quartz/quartz.config.yaml

            - name: Inject style overrides
              run: |
                  echo 'div.center table { margin-left: 0; margin-right: 0; }' >> quartz/quartz/styles/custom.scss
                  echo 'body .search > .search-container > .search-space > .search-layout { background-color: var(--background-secondary); }' >> quartz/quartz/styles/custom.scss

            - uses: actions/setup-node@v4
              with:
                  node-version: 24

            - name: Cache dependencies
              uses: actions/cache@v4
              with:
                  path: ~/.npm
                  key: ${{ runner.os }}-node-${{ hashFiles('quartz/package-lock.json') }}
                  restore-keys: |
                      ${{ runner.os }}-node-

            - name: Cache Quartz plugins
              uses: actions/cache@v4
              with:
                  path: quartz/.quartz/plugins
                  key: ${{ runner.os }}-plugins-${{ hashFiles('quartz/quartz.lock.json') }}
                  restore-keys: |
                      ${{ runner.os }}-plugins-

            - name: Install dependencies
              run: npm ci
              working-directory: quartz

            - name: Install Quartz theme
              run: npm install @quartz-themes/terminal2k
              working-directory: quartz

            - name: Install Quartz plugins
              run: npx quartz plugin install
              working-directory: quartz

            - name: Build site
              run: npx quartz build
              working-directory: quartz

            - name: Upload artifact
              uses: actions/upload-pages-artifact@v3
              with:
                  path: quartz/public

    deploy:
        needs: build
        environment:
            name: github-pages
            url: ${{ steps.deployment.outputs.page_url }}
        runs-on: ubuntu-latest
        steps:
            - name: Deploy to GitHub Pages
              id: deployment
              uses: actions/deploy-pages@v4
